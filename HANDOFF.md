# 引き継ぎ文：呉服Quiz工房

## このドキュメントの位置づけ

前回のセッション（令和方針へのシリーズ作り直し・多コース化・UI配色・コース1設問作成）の成果物です。
**アプリは実装済み。`feature/beyond-mvp` ブランチで作業中。開発サーバーは停止済み。**

### AIとの共同作業の範囲
- **共同**: スキル作成・動作確認・バグ修正・UI改善・プロンプト調整・Quiz工房のブラッシュアップ・ガイドと設問の叩き台
- **制作者が単独で行う**: ガイド・設問の最終判断・画面キャプチャ・図解作成・提出
- **設問の言い回し微修正・再グリル**: 提出後でよい（今セッション合意）

**次のチャットは「コース2（規定演技）のレッスン作成」から始めてください。**
`pnpm dev` を起動し、「コース2のレッスンを作る」または `create-lesson-questions` と話しかける。

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| システム名 | 呉服Quiz工房（制作スタジオ）。画面上部の表記は `Gofuku Quiz 工房` |
| 成果物の名称 | 令和きもの販売員ドリル（シリーズ名は「格とTPO」） |
| 制作者 | 百貨店呉服部の部門長（本人のみ） |
| 期限 | 2026年8月28日 |
| 提出物 | 図解のURL（スクリーンショット＋説明。アプリのURLは不要） |

---

## 技術スタック

- **フレームワーク**: Next.js 16 + Tailwind CSS 4
- **AI**: Anthropic Claude API（`@anthropic-ai/sdk`）。モデル: `claude-opus-4-5`
- **起動**: `pnpm dev`（ワークスペースルートで実行）→ http://localhost:3000
- **APIキー**: 画面の設定欄（⚙）に貼るだけ。ファイルに書かない・保存されない
- **データ**: SQLite（`data/app.db`）+ Drizzle ORM で管理。`pnpm seed` で初期データ投入（`data/kakuto-tpo/courses/` を読む）

---

## ファイル構成

```
Graduation-work/
├── app/
│   ├── api/
│   │   ├── data/route.ts           GET  全データ（series + courses[]）
│   │   ├── save-guide/route.ts     POST シリーズ/コースガイド保存（コースは courseId 必須）
│   │   ├── generate/route.ts       POST Claude API でレッスン生成（同一コースの承認済みを参照）
│   │   ├── save-lesson/route.ts    POST 設問保存（検査再実行。未承認なら status=draft）
│   │   └── approve-lesson/route.ts POST「これでよい」承認
│   ├── preview/[lessonId]/page.tsx プレビュー（courses からレッスンを探す）
│   ├── globals.css                 スタジオ配色トークン（--color-studio-*）
│   ├── layout.tsx
│   └── page.tsx                    タイトルバー + 3ペイン（幅 2:4:4）
├── data/
│   ├── app.db                      SQLite（git管理外）
│   └── kakuto-tpo/
│       ├── series.json             シリーズガイド（令和方針・保存済み）
│       ├── courses/
│       │   ├── 01-map/             これから学ぶための地図（ガイド＋レッスン3本 approved）
│       │   ├── 02-kitei/           規定演技（第一礼装）（ガイド記入済・設問未生成）
│       │   ├── 03-aida/            あいだ（訪問着・付下・色無地）
│       │   ├── 04-jiyu/            自由演技
│       │   └── 05-chosei/          格を調整して提案する
│       └── kimono-kaku/            旧1コース構成のシード（参照用・seedは読まない）
├── lib/
│   ├── types.ts                    DrillData.courses: Course[]
│   ├── data.ts                     loadAll / saveCourseGuide(courseId) / findLesson
│   ├── inspect.ts                  禁止語はガイドと同期（ドレスコード等＋燕尾服等）
│   ├── prompt.ts                   コースタイトル付きプロンプト
│   └── db/schema.ts                courses.courseIndex あり
├── scripts/seed.ts                 courses/ 配下を投入（テーブルを DROP して作り直す）
└── .cursor/skills/                 create/review-series/course-guide、create-lesson-questions は多コース対応済み
```

---

## コンテンツの4階層

```
令和きもの販売員ドリル（総称）
└ シリーズ  格とTPO
   ├ シリーズガイド（令和方針で書き直し済み）
   ├ コース1  これから学ぶための地図        レッスン3本 ●●●
   ├ コース2  規定演技（第一礼装）          レッスン3本 □□□
   ├ コース3  あいだ（訪問着・付下・色無地） レッスン3本 □□□
   ├ コース4  自由演技                      レッスン3本 □□□
   └ コース5  格を調整して提案する          レッスン3本 □□□

コース1レッスン
├ map-01  なぜ格があるか
├ map-02  規定演技と自由演技
└ map-03  必需品から選択品へ

コース2レッスン（未生成）
├ kitei-01  黒留袖と主催側
├ kitei-02  振袖
└ kitei-03  紋付と黒紋付

各レッスン
├ 状態: 未生成 / 下書き / これでよい
├ 検査: 未実施 / 合格 / 不合格（理由）
└ 設問 × 8（四択 / ○×。手順タイプはMVP外）
```

---

## UIの画面構成

```
┌─────────────────────────────────────────────────────────────┐
│              Gofuku Quiz 工房（中央揃え・細いバー）            │
├──────────┬────────────────────┬─────────────────────────────┤
│ P1 ツリー │ P2 ガイド           │ P3 編集 / 設問               │
│ 幅 2      │ 幅 4  #F2EBEB      │ 幅 4  #F2EBEB               │
│ slate-800 │                    │                             │
│▼ 格とTPO │ シリーズ/コース     │ 設問一覧 [生成] [これでよい] │
│ ▼ コース │ ガイド編集          │ [プレビュー]                │
│  ● / □  │                    │                             │
│[⚙設定]   │                    │                             │
└──────────┴────────────────────┴─────────────────────────────┘
```

- タイトルバー: `#5B7A9A` / 左ペイン: 当初の `slate-800` / 中央・右: `#F2EBEB`
- 項目名（目的など）: ネイビー `#1E3A5F`
- 生成ロックは **同一コース内** の前レッスンが approved になるまで（コースをまたがない）

P4（AIチャット壁打ち）は **後日追加**。

---

## 今セッションで確定した方針（ドリル全体を貫く）

- **第一原理**: 令和のフォーマルきものは必需品ではなく、場・規模・当事者の意向で選ぶ **選択品**。`purpose` 先頭と `exceptions` 冒頭に置く。
- **唯一のメタファー**: 規定演技 / 自由演技。「段」は補足。問題は「何段か」ではなく「規定寄りか自由寄りか、どう調整するか」。
- **修了時の3点**: (A) 調整して提案 (B) 規定と自由を見分け代表を言える (C) なぜ格があるかを語れる。コース1は地図（概念の存在）。
- **歴史**（明治〜大正に洋装の礼法へ適応して格付けが再構築された）は **ガイドにだけ書く**。問題の正解根拠にも洋装比喩にもしない。禁止語にドレスコード・燕尾服・タキシード・カクテルドレス。
- **弔事**: コース2で黒紋付を規定側の代表として出すだけ。選択品シナリオは慶事に限る。
- **色留袖**: コース2では五つ紋のみ。紋の数による上げ下げはコース5で再登場。
- **文体**: シナリオ主・知識確認は導入に少数。舞台は百貨店呉服売り場。語尾は「〜か。」「〜である。」
- **画面の顔**: 令和きもの販売員ドリル。シリーズ名「格とTPO」はツリー上のシリーズ。工房名はタイトルバー。

---

## 6工程（レッスン1本を作る手順）

1. シリーズガイド＋コースガイドを書く（P1でシリーズ/コースを選択 → P2で記入 → [保存]）
2. レッスンを選んで [このレッスンを生成]（APIキーが必要）。エージェントが設問を書いて `save-lesson` してもよい
3. プログラムが形式を自動検査する（8問・四択/○×形式・解説・禁止語）
4. 悪い設問だけ P1 でクリック → P3 で手直し
5. 検査合格後に [これでよい ✓]（同一コースの次レッスン生成の参照元になる）
6. [プレビュー ▶] で8問を解く（検品用。点数は保存されない）

---

## 機械検査の内容

| 検査項目 | ルール |
|---|---|
| 設問数 | 8問であること |
| 四択 | 選択肢が4つ・正解インデックスが0〜3 |
| ○× | answerが true か false |
| 解説 | 全設問にあること |
| 禁止語 | フォーマル度・ドレスコード・ランク・レベル・格式・ステータス・燕尾服・タキシード・カクテルドレス |

呉服の内容の正しさ・再登場の確認は **制作者が目視**で行う。

---

## 現在の状態

| 項目 | 状態 |
|---|---|
| Next.js / 3ペイン / 生成 / 検査 / 編集 / 承認 / プレビュー | ✅ |
| 多コースUI（`courses[]`・ツリー展開・courseId付き保存） | ✅ |
| スタジオ配色（タイトル / 左スレート / 中央右ベージュ） | ✅ とりあえずOK |
| シリーズガイド（令和方針） | ✅ DB + `series.json` |
| 5コースの枠とコースガイド叩き台 | ✅ `data/kakuto-tpo/courses/` |
| コース1 設問（map-01〜03） | ✅ 各8問・検査合格・approved |
| コース2〜5 設問 | ❌ 未生成（pending） |
| 旧レッスン1（カタログ型・第一礼装） | 破棄方針。`kimono-kaku/` に残るが seed 対象外 |
| スクリーンショット / 図解提出 | ❌ まだ |
| 開発サーバー | 停止済み |

---

## 次のチャットでやること（優先順）

1. **コース2のレッスン作成**（黒留袖と主催側 → 振袖 → 紋付と黒紋付）。`create-lesson-questions`。選択品前提の慶事シナリオ。色留袖は五つ紋のみ。弔事は黒紋付を代表として出すだけ
2. **コース3〜5のレッスン作成**（あいだ → 自由演技 → 調整して提案）。色留袖の紋の数はコース5で再登場
3. 必要なら **ガイドのレビュー**（`review-series/course-guide`）
4. **UIの微調整**（配色は「とりあえずOK」。必要なら相談）
5. **LLM のアプリ内実装**（Agents Bar、最後）
6. **以降は制作者が単独で実施**（キャプチャ・図解・提出）。設問の言い回しグリルは提出後可

---

## 未決事項

- 各コースのレッスン本数・タイトルの最終確定（今は各3本の叩き台）
- コース2〜5ガイド本文の詰め（叩き台は入っている）
- Quiz Studio の 6本柱の具体的な中身（参照スキルに記載なし・照合未完了）
- 図解の提出形式・提出先（講師から指定があるはず）
- 慶事以外の場面別コース（弔事専門・茶道など）はこのシリーズに足さない。別判断
- Anthropic APIキーは取得済み（生成ボタン用。今セッションのコース1はエージェントが設問を直接保存した）

---

## 既知の制約・注意事項

- **APIキーはページを閉じると消える**（ファイル保存なし。意図的な設計）
- **スクリーンショット時は DevTools を閉じる**（ネットワークタブにキーが見える）
- **モデル名**: `claude-opus-4-5`（`lib/prompt.ts` の `MODEL` 定数で変更可）
- **生成ロック**: 同一コース内の前レッスンが「これでよい」になるまで、そのコースの次レッスンは生成ボタン不可
- **現在のブランチ**: `feature/beyond-mvp`（提出時は `git checkout main` で戻る）
- **DBはgit管理外**: `data/app.db` はブランチをまたいで共有。シード元JSONを直したら `pnpm seed` でDBへ反映（**既存DBをDROPして入れ直す**）
- **save-guide のコース保存**は `{ type: "course", courseId, guide }`。`courseId` なしは不可
- 旧フォルダ `data/kakuto-tpo/kimono-kaku/` は残しているが seed は `courses/` だけ読む

---

## 後日追加予定（MVP外）

- P4 AI壁打ちチャット（ガイド内容や設問について Claude に相談）
- 設問タイプ：手順（3〜4項目を正しい順に並べる）
- レスポンシブ対応
