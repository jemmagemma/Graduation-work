# 引き継ぎ文：呉服Quiz工房

## このドキュメントの位置づけ

今回のセッション（ドリルデモをグリルし、`/drill` の薄い学習者シェルを実装。Neon は見送り）の成果物です。
**アプリは実装済み。`feature/beyond-mvp` ブランチ。** 変更は未コミット。

### AIとの共同作業の範囲
- **共同**: スキル作成・動作確認・バグ修正・UI改善・プロンプト調整・Quiz工房のブラッシュアップ・ガイドと設問の叩き台・学習者シェル
- **制作者が単独で行う**: ガイド・設問の最終判断・画面キャプチャ・図解作成・提出
- **設問の言い回し**: コース1（map-01〜03）・コース2（kitei-01〜03）・コース3（aida-01〜03）は承認済み。コース4（jiyu-01 紬と素材）は提出後。固定認識は `.cursor/skills/create-lesson-questions/locked-principles.md`

**次のチャットはドリルの5画面をブラウザで通し、図解用キャプチャに進むところから。**
コース4のグリルは提出後。map・kitei・aida は承認済み。無断で直さない。

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| システム名 | 呉服Quiz工房（制作スタジオ）。画面上部の表記は `Gofuku Quiz 工房` |
| 成果物の名称 | 令和きもの販売員ドリル（シリーズ名は「品目の格とTPO」） |
| 制作者 | 百貨店呉服部の部門長（本人のみ） |
| 期限 | 2026年8月28日 |
| 提出物 | 図解のURL（スクリーンショット＋説明。アプリのURLは不要） |

---

## 技術スタック

- **フレームワーク**: Next.js 16 + Tailwind CSS 4
- **AI**: Anthropic Claude API（`@anthropic-ai/sdk`）。モデル: `claude-opus-4-5`
- **起動**: `pnpm dev`（ワークスペースルートで実行）→ http://localhost:3000
- **APIキー**: 画面の設定欄（⚙）に貼るだけ。ファイルに書かない・保存されない
- **データ**: SQLite（`data/app.db`）+ Drizzle ORM で管理。`pnpm seed` で初期データ投入（`data/kakuto-tpo/courses/` を読む）

---

## ファイル構成

```
Graduation-work/
├── app/
│   ├── api/
│   │   ├── data/route.ts           GET  全データ（series + courses[]）
│   │   ├── save-guide/route.ts     POST シリーズ/コースガイド保存（コースは courseId 必須）
│   │   ├── generate/route.ts       POST Claude API でレッスン生成。scope は all | failed（不合格のみ）
│   │   ├── save-lesson/route.ts    POST 設問保存（検査再実行。未承認なら status=draft）
│   │   └── approve-lesson/route.ts POST「これでよい」承認
│   ├── drill/
│   │   ├── layout.tsx
│   │   ├── page.tsx                シリーズ地図（コース1〜3は入る。4〜5は準備中）
│   │   └── [courseId]/
│   │       ├── page.tsx            レッスン一覧（approvedのみ。未公開は準備中）
│   │       └── [lessonId]/page.tsx 解く画面（LessonPlayer mode=drill）
│   ├── preview/[lessonId]/page.tsx LessonPlayer mode=preview（工房の検品用）
│   ├── globals.css                 スタジオ配色トークン（--color-studio-*）
│   ├── layout.tsx
│   └── page.tsx                    タイトルバー + 3ペイン。生成後は合格を全文表示。不合格は手修正／不合格のみ／全体
├── components/
│   ├── drill-shell.tsx             学習者画面の細いタイトルバー
│   └── lesson-player.tsx           preview と drill で共用
├── data/
│   ├── app.db                      SQLite（git管理外）
│   └── kakuto-tpo/
│       ├── series.json             シリーズガイド（品目の格とTPO・保存済み）
│       ├── courses/
│       │   ├── 01-map/             これから学ぶための地図（ガイド＋レッスン3本 approved）
│       │   ├── 02-kitei/           規定演技（第一礼装）。ガイド＋lesson-01〜03.json も approved（問数 8 / 5 / 5）
│       │   ├── 03-aida/            あいだ。ガイド問数 [6, 6, 6]。lesson_roles に付下・色無地の6判断。lesson-01〜03 は approved
│       │   ├── 04-jiyu/            自由演技
│       │   └── 05-chosei/          格を調整して提案する
│       └── kimono-kaku/            旧1コース構成のシード（参照用・seedは読まない）
├── lib/
│   ├── types.ts                    DrillData.courses: Course[]
│   ├── data.ts                     loadAll / saveCourseGuide(courseId) / findLesson
│   ├── drill.ts                    approved 抽出・次レッスン・DRILL_CATCH
│   ├── inspect.ts                  禁止語（格式・ドレスコードは対象外。セミフォーマルは禁止）＋四択文長＋宣言問数
│   ├── questionCount.ts            コースガイド question_counts
│   ├── prompt.ts                   生成ルール。不合格のみ再生成時は残す設問と作り直す設問を載せる
│   ├── references.ts              レッスン名で参照を選ぶ。振袖は hare-ke。訪問着・付下・色無地は hatsugama と kekkonshiki。地図・自由は kajuaru。色無地／小紋は edo-sanyaku。紬は tsumugi-omeshi・sozai-kaku-teire・dansei-kimono。紋は kamon-chado と kamon-kiso。帯は obi-kakuawase。小物は obijime-obiage。紋付は dansei-kimono
│   └── db/schema.ts                courses.courseIndex あり
├── scripts/seed.ts                 courses/ 配下を投入（テーブルを DROP して作り直す）
└── .cursor/skills/                 6本。正本は create-lesson-questions/locked-principles.md
```

---

## コンテンツの4階層

```
令和きもの販売員ドリル（総称）
└ シリーズ  品目の格とTPO
   ├ シリーズガイド（思想＋初手の範囲・保存済み）
   ├ コース1  これから学ぶための地図        レッスン3本 ●●●
   ├ コース2  規定演技（第一礼装）          レッスン3本 ●●●
   ├ コース3  あいだ（訪問着・付下・色無地） レッスン3本 ●●●
   ├ コース4  自由演技                      レッスン3本 □□□
   └ コース5  格を調整して提案する          レッスン3本 □□□

コース1レッスン（問数 8, 8, 4）
├ map-01  なぜ格があるか                 8問 approved
├ map-02  規定演技と自由演技             8問 approved
└ map-03  持っていて当然ではない         4問 approved（仕事は前提合わせ。所有は背景）

コース2レッスン（問数 [8, 5, 5]）
├ kitei-01  黒留袖と主催側     8問 approved（Q5 は第一礼装として扱う。名称正解は解消）
├ kitei-02  振袖               5問 approved（ハレ／ケ・大発会は背景。語は写していない）
└ kitei-03  紋付と黒紋付       5問 approved（弔事は代表として出すのみ。Q1 は息子の結婚式に父。01の母と近い。コピーするな）

コース3レッスン（問数 [6, 6, 6]）
├ aida-01  訪問着              6問 approved（工房再生成した稿。Q3は紋、Q4は絵羽。方針と食い違う。無断修正しない）
├ aida-02  付下                6問 approved（四択のみ。DBから JSON に写して seed 済み。色無地がダミーに出る。準礼装・略礼装が解説に出る。無断修正しない）
└ aida-03  色無地              6問 approved（四択のみ。2問再生成のあと承認。Q4は友人の結婚式で aida-01 と近い。無断修正しない）

各レッスン
├ 状態: 未生成 / 下書き / これでよい
├ 検査: 未実施 / 合格 / 不合格（理由）
└ 設問数はコースガイド question_counts（8問固定ではない。四択 / ○×。付下・色無地は四択のみ。手順タイプはMVP外）
```

---

## UIの画面構成

```
┌─────────────────────────────────────────────────────────────┐
│              Gofuku Quiz 工房（中央揃え・細いバー）            │
├──────────┬────────────────────┬─────────────────────────────┤
│ P1 ツリー │ P2 ガイド           │ P3 編集 / 設問               │
│ 幅 2      │ 幅 4  #F2EBEB      │ 幅 4  #F2EBEB               │
│ slate-800 │                    │                             │
│▼ 品目の格 │ シリーズ/コース     │ 設問一覧 [生成] [これでよい] │
│  とTPO   │ ガイド編集          │ [プレビュー]                │
│ ▼ コース │                    │ 合格設問は全文表示            │
│  ● / □  │                    │ 不合格は手修正／不合格のみ／全体│
│[⚙設定]   │                    │                             │
└──────────┴────────────────────┴─────────────────────────────┘
```

- タイトルバー: `#5B7A9A` / 左ペイン: 当初の `slate-800` / 中央・右: `#F2EBEB`
- 項目名（目的など）: ネイビー `#1E3A5F`
- 生成ロックは **同一コース内** の前レッスンが approved になるまで（コースをまたがない）
- 生成ボタンは `onClick={() => onGenerate('all')}`。イベントを scope に渡すと `JSON.stringify` が壊れる（修正済み）

P4（AIチャット壁打ち）は **後日追加**。

学習者画面（工房 `/` とは別。`http://localhost:3000/drill`）

```
/drill  シリーズ地図
  令和きもの販売員ドリル
  品目の格とTPO
  キャッチ1行（ガイド本文は出さない）
  コース1〜3 入る → /drill/{courseId}
  コース4〜5 準備中（リンクなし）

/drill/{courseId}  レッスン一覧
  承認済みのみ。未公開コースは「準備中」

/drill/{courseId}/{lessonId}  解く
  プレビュー表記なし。完了後はコースに戻る / もう一度 / 次レッスン（任意）
```

工房の［プレビュー］は `/preview/{lessonId}` のまま（プレビュー表記・閉じる）。

---

## 今セッションで確定した方針（ドリル全体を貫く）

- **格の定義**: ここでの格は、品目がTPOにおいて持つ位であり、品格や値段ではない。
- **シリーズ名**: 「品目の格とTPO」。色留袖・付下などを指す語は **品目**。アイテムは使わない。
- **思想**: ①売場の新たな仲間を育てる ②クイズできものの概念を体験的に身につける。②はドリル全体。**このシリーズはその初手**（品目の格とTPOまで）。
- **貫く前提（接客）**: 場・規模・意向を聞いて、その都度提案する。設問の軸になる伺いは、会場・雰囲気・TPO・和装にしたい理由。短い正解文（場所や雰囲気を伺う）は同じ伺いの短縮であり、別正解ではない。「選択品」は使わない。
- **コース1レッスン3**: タイトルは「持っていて当然ではない」。仕事は前提合わせ。所有や昭和様式は背景。所有そのものを問うな。設問は4問。
- **コース1の三本柱**: (1) 成り立ちはレッスン1で事実1〜2問 (2) なぜ決まりがあるか（レッスン2の規定／自由は例え） (3) 現代の前提合わせはレッスン3。
- **表記**: 売場。「売り場」は使わない。map-01 Q6 は「売場」に直済み。
- **正解番号**: 会話は1始まり。JSON の `correct` だけ0始まり。
- **規定演技／自由演技**: 例え。正解の名称にしない。kitei-01 Q5 は「第一礼装として扱う」に直済み。
- **コース2の問数**: `[8, 5, 5]`。薄い問で8に伸ばさない。コース3以降も生成前に問数を詰める。
- **品目そのもの**: 見た目の識別（絵羽・柄の位置・縫い目）は別シリーズ。制作者が用意する予定。このシリーズのコース3は、規定でも自由でもないあいだとしての位置づけと、場に応じた仮説だけを問う。
- **コース3の問数**: `[6, 6, 6]`。3本とも四択のみ（○×なし）。DB と `03-aida/course.json` は更新済み。
- **aida-01**: 工房再生成の6問が approved。DB と `lesson-01.json` は `pnpm seed` 済み。方針（絵羽・紋を出さない）と、承認稿の Q3（紋を入れない理由）・Q4（絵羽）は食い違う。無断修正しない。
- **aida-02 付下の仕事**: あいだの中の控えめ側と、一歩引く場の仮説。あいだであることの再確認はしない（aida-01でやった）。訪問着の焼き直しはしない。
- **aida-02 の6判断**: (1) 控えめなあいだ（第一礼装でも洒落ものでもない）(2) 卒入では一歩引く仮説として付下を立てる (3) 卒入で訪問着を希望されたら場のトーンを返す (4) 初釜は喜びの席なので訪問着も残り、場の名では閉じない (5) 会社パーティは雰囲気・規模のグレーゾーン (6) 控えめでも自由側ではない。
- **aida-02 の対比**: 訪問着は対比まで。禁止にしない。正解は場のトーンを返したうえで付下を仮説にする。「何段下か」は問わない。対比の相手は小紋・紬。色無地は設問に出さない。
- **aida-02 で出さない**: 絵羽・縫い目・柄の位置・紋の数・色無地・振袖・未婚。○×は使わない。
- **会社パーティ**: 付下のグレーとして残す。map-03 の「会社のパーティに訪問着で」との重なりは出稿時に目視（コピーではないと判断した）。
- **コース3ガイドの直し方**: `lesson_roles` にレッスン2・3の段落を足した。`must_include` の共通文は残した。DB は `save-guide` 済み。
- **aida-03 色無地の仕事**: 無地であることと、紋の有無で場が動く入口。あいだであることの再確認はしない。紋の数（一つ／三つ／五つ）の運用はコース5。
- **aida-03 の6判断**: (1) 紋なしは場が狭まる (2) 紋なしを自由側（小紋・紬）に落とさない (3) 通常の茶席では一つ紋を仮説にする。名だけでは決めつけない（地図レッスン3のコピーにはしない） (4) 卒入は無地と一つ紋で立てる。付下との優劣は問わない (5) 一つ紋と結婚式は場と立場を返す。絶対では閉じない (6) 江戸三役は遠目無地＋一つ紋で近い場。形状は問わない。
- **aida-03 の対比**: 訪問着と小紋・紬。付下は背景。色留袖は出さない。一つ紋は出してよい。三つ／五つは出さない。
- **aida-03 で出さない**: 絵羽・縫い目・柄の位置・紋の数の上げ下げ・三つ紋・五つ紋・色留袖・初釜・振袖・未婚。○×は使わない。茶席の紋の細かい運用はコース5。
- **aida-03 の誤問**: 最大1問（判断3の茶席か判断5の結婚式グレーの厚い方）。記述の正誤カタログは不可。
- **誤っているものを選ばせる四択**: ドリル方針として許可。売場の誤った判断を1つ選ばせるシナリオに限る。記述の正誤カタログは不可。各レッスンで多くても1問。無くてよい。承認済みは遡及しない。付下では判断3か5の厚い方を生成時に選ぶ。`series.json` の文体と `locked-principles.md` に書いた。
- **禁止語**: 検査（`lib/inspect.ts`）とシリーズガイドを同じにする。フォーマル度・ランク・レベル・ステータス・セミフォーマル・燕尾服・タキシード・カクテルドレス。格式は対象外のまま。
- **ドレスコード**: 禁止から外した。問題文の引用と解説で平服の意味を返してよい。正解の名称にするな。
- **ランク**: 残す。ワンランクは「一段緩める」。準礼装・略礼装は解説の補足。主レンズにしない。「何段か」を問うな。
- **ハレ／ケ・大発会**: `data/references/hare-ke-daihakkai-furisode.txt`。振袖生成に載る。語や証券儀礼は写さない。使う事実は「未婚女性の第一礼装として節目の場を整える」まで。
- **初釜と卒入**: `hatsugama-sotsunyugakushiki.txt`。訪問着・付下・色無地の生成に載る。初釜は喜びの席（訪問着・付下が仮説）。卒入の母親は一歩引く（付下・色無地が仮説）。菓子・貴金属・着付けは背景。場の名だけで決めつけるな。
- **結婚式の立場**: `kekkonshiki-tachiba.txt`。コース2〜3の生成に載る。招く側は第一礼装、招かれる側は訪問着、平服は普段着ではない。帯・小物の本論はコース5。
- **誰が主役か**: `kajuaru-fomaru-shuyaku.txt`。コース1・4の生成に載る。訪問着の生成には載せない（資料が訪問着をフォーマル側に置くため。ガイドではあいだ）。使う事実は主役の所在まで。ブーツ・セーターは対象外。着物警察は背景。
- **江戸三役**: `edo-sanyaku.txt`。色無地（aida-03）と小紋（jiyu-02）の生成に載る。文様の形状暗記は背景。準礼装・略礼装・セミフォーマルは写さない。
- **紬とお召**: `tsumugi-omeshi.txt`。jiyu-01 に載る。高価でも普段着・おしゃれ着。禁止・マナー違反で止めず、素材と目的で説明する。
- **素材と格**: `sozai-kaku-teire.txt`。jiyu-01 に載る。手入れは背景。抄録が綿の途中で途切れている。
- **家紋と茶席**: `kamon-chado.txt`。chosei-01 のみ。コース2〜4には載せない。
- **家紋の基礎**: `kamon-kiso.txt`。chosei-01 で kamon-chado と併用。色留袖の三つ紋運用はコース5。コース2の色留袖は五つ紋のみ。
- **帯の格合わせ**: `obi-kakuawase.txt`。chosei-02 のみ。ジーンズ比喩は写さない。訪問着はガイドではあいだ。
- **帯締め・帯揚げ**: `obijime-obiage.txt`。chosei-03 のみ。価格・組み方暗記は背景。調和は場と意向に翻訳する。
- **男性の着物**: `dansei-kimono.txt`。kitei-03 と jiyu-01 に載る。格は紋の数・羽織袴・素材。セミフォーマルは写さない。kitei-03 は承認済みなので再生成や照合用。
- **生成後の検品UI**: 検査合格の設問は問題文・選択肢・解説をその場に出す。不合格は手修正／不合格のみ再生成／全体を再生成。不合格の設問が無い（問数だけ違う等）ときは不合格のみは押せない。
- **画面承認のあと**: API は DB だけを更新する。エージェントは同じ設問を JSON にも書く。kitei-02 / 03 は後から写し済み。
- **成り立ち**: 明治〜大正に洋装の礼法へ適応して再構築された、を学習者と共有する。比喩は規定／自由のまま。「相当する洋装はどれか」は作らない。禁止語に燕尾服・タキシード・カクテルドレス。
- **TPOの格の決め方**（map-01 Q1）: TPOの格は品目と帯・小物の取り合わせで決まる。敬意は目的であり、値段の対にはしない。
- **コース1の「格」の書き方**: 「品目の格」または「TPOの格／TPO上の格」。裸の「格」だけで問わない。規定／自由はレッスン1に出さない。
- **答え漏らし**: 問題文が正解の手がかりを先に置かない。続柄を書いて「関係を聞け」と問わない。同じ固有シナリオをコース内で繰り返さない。
- **唯一のメタファー**: 規定演技 / 自由演技。「段」は補足。
- **修了時の3点**: (A) 調整して提案 (B) 規定と自由を見分け代表を言える (C) なぜ格があるかを語れる（成り立ちと理由を添えて）。
- **弔事**: コース2で黒紋付を規定側の代表として出すだけ。場と意向を聞いて選ぶシナリオは慶事に限る。
- **色留袖**: コース2では五つ紋のみ。紋の数による上げ下げはコース5で再登場。
- **四択の文長**: 4文は同じ構文・同程度の長さ。正解にだけ理由を足して長くしない。理由は解説へ。機械検査は正解が他より8文字以上長いと不合格。
- **画面の顔**: 令和きもの販売員ドリル。シリーズ名はツリー上。工房名はタイトルバー。
- **提出まではドリルデモを先にする。** コース4の生成は提出後。
- **Neon には今移さない。** localhost + 既存 SQLite。他人が URL で触るときに初めて見直す。
- **学習者入口は `/drill`。** `/` は工房のまま。
- **出す文言はコース名・レッスン名・キャッチだけ。** ガイド本文は出さない。キャッチは `lib/drill.ts` の `DRILL_CATCH`（売場の新たな仲間へ。クイズで、品目の格とTPOの初手を身につける。）。
- **配色は工房と同じトークン。** レイアウトだけカード型。3ペインと⚙は置かない。
- **点数は保存しない。** 新しいテーブルは作らない。
- **デモ完了条件:** (1) 地図 (2) コース1一覧 (3) 未回答 (4) 正誤＋解説 (5) 完了。これ以上の入口装飾は足さない。

---

## 6工程（レッスン1本を作る手順）

1. シリーズガイド＋コースガイドを書く（P1でシリーズ/コースを選択 → P2で記入 → [保存]）
2. レッスンを選んで [このレッスンを生成]（APIキーが必要）。エージェントが設問を書いて `save-lesson` してもよい
3. プログラムが形式を自動検査する（宣言問数・四択/○×形式・解説・禁止語・四択文長）。合格設問はP3に全文表示。不合格は手修正／不合格のみ再生成／全体を再生成
4. 悪い設問だけ手修正。エージェントはユーザーが認めるまで保存しない
5. 検査合格後に [これでよい ✓]（同一コースの次レッスン生成の参照元になる）。エージェントは承認内容を JSON にも書く
6. [プレビュー ▶] で宣言問数を解く（検品用。点数は保存されない）

---

## 機械検査の内容

| 検査項目 | ルール |
|---|---|
| 設問数 | コースガイド `question_counts[lessonIndex]` と一致。未宣言は不合格 |
| 四択 | 選択肢が4つ・正解インデックスが0〜3 |
| 四択の文長 | 正解が他のどの選択肢より8文字以上長いと不合格 |
| ○× | answerが true か false |
| 解説 | 全設問にあること |
| 禁止語 | フォーマル度・ランク・レベル・ステータス・セミフォーマル・燕尾服・タキシード・カクテルドレス |

呉服の内容の正しさ・再登場の確認は **制作者が目視**で行う。

---

## 現在の状態

| 項目 | 状態 |
|---|---|
| Next.js / 3ペイン / 生成 / 検査 / 編集 / 承認 / プレビュー | ✅ |
| 多コースUI（`courses[]`・ツリー展開・courseId付き保存） | ✅ |
| スタジオ配色（タイトル / 左スレート / 中央右ベージュ） | ✅ とりあえずOK |
| シリーズガイド（品目の格とTPO・思想＋初手） | ✅ DB + `series.json`。誤問（誤判断を選ばせる四択）のルールを文体に追記済み |
| 5コースの枠とコースガイド | ✅ 「選択品」はガイドから除去済み |
| コース1 設問（map-01〜03） | ✅ approved。問数 8 / 8 / 4。L3 は前提合わせ（家の本の決めつけ→場所や雰囲気を伺う。正解は2） |
| 禁止語（検査＋シリーズ） | ✅ ドレスコードを外した。セミフォーマルを足した。ランク・フォーマル度は残す |
| コース2 kitei-01〜03 | ✅ approved。問数 8 / 5 / 5。DB と `lesson-01〜03.json` は揃っている。kitei-01 Q5 の名称正解は解消。kitei-03 Q1（息子の結婚式に父）は残件。無断修正しない |
| 生成後UI（合格全文・不合格は手修正／範囲付き再生成） | ✅。生成ボタンはイベントを scope に渡さない |
| 参照 `hare-ke-daihakkai-furisode.txt` | ✅ 振袖テーマで生成に載る |
| 参照 `hatsugama-sotsunyugakushiki.txt` | ✅ 訪問着・付下・色無地の生成に載る。初釜と卒入の場。段名は写さない |
| 参照 `kekkonshiki-tachiba.txt` | ✅ コース2〜3の生成に載る。結婚式の立場と平服。ドレスコードは引用と解説可 |
| 参照 `kajuaru-fomaru-shuyaku.txt` | ✅ コース1・4の生成に載る。誰が主役か。訪問着をフォーマル側に寄せるな |
| 参照 `edo-sanyaku.txt` | ✅ 色無地・小紋の生成に載る。江戸三役。段名・セミフォーマルは写さない |
| 参照 `tsumugi-omeshi.txt` | ✅ jiyu-01 に載る。紬は高価でも普段着。お召は織りの中で格が高い |
| 参照 `sozai-kaku-teire.txt` | ✅ jiyu-01 に載る。素材と格。抄録は綿の途中まで。手入れは背景 |
| 参照 `kamon-chado.txt` | ✅ chosei-01 のみ。紋の数と茶席。コース2〜4には載せない |
| 参照 `kamon-kiso.txt` | ✅ chosei-01 で kamon-chado と併用。家紋の基礎。コース2〜4には載せない |
| 参照 `obi-kakuawase.txt` | ✅ chosei-02 のみ。帯の格合わせ。ジーンズ比喩は写さない |
| 参照 `obijime-obiage.txt` | ✅ chosei-03 のみ。帯締め・帯揚げ。お茶席の調和 |
| 参照 `dansei-kimono.txt` | ✅ kitei-03 と jiyu-01。男性の着物。セミフォーマルは写さない |
| コース3 aida-01（訪問着） | ✅ approved。問数6。DB と `lesson-01.json` は揃っている。工房再生成稿。Q3（紋）・Q4（絵羽）は「品目そのものは別シリーズ」と食い違う。無断修正しない |
| コース3 aida-02（付下） | ✅ approved。問数6・四択のみ。DB と `lesson-02.json` は揃っている（今回 JSON に写して seed）。色無地がダミーに出る。準礼装・略礼装が解説に出る。無断修正しない |
| コース3 aida-03（色無地） | ✅ approved。問数6・四択のみ。DB と `lesson-03.json` は揃っている。2問再生成のあと承認。Q4は友人の結婚式で aida-01 と近い。無断修正しない |
| コース4〜5 | ❌ 未生成。問数は `[8, 8, 8]` 仮置き。生成は提出後。生成前にグリルする |
| 学習者シェル `/drill` | ✅ 地図・一覧・解く画面。approved のみ。コース4〜5は準備中 |
| LessonPlayer 共用 | ✅ `components/lesson-player.tsx`。preview / drill |
| Neon / Vercel | ❌ しない（提出は図解。アプリURL不要） |
| ドリルの解答〜完了の目視 | △ HTTP では地図・一覧・未回答まで確認。クリック操作は未確認 |
| スキル6本 | ✅ `.cursor/skills/`。正本は `locked-principles.md`。API は DB のみ、JSON はエージェントが書く |
| 旧レッスン1（カタログ型・第一礼装） | 破棄方針。`kimono-kaku/` に残るが seed 対象外 |
| スクリーンショット / 図解提出 | ❌ まだ |

---

## 次のチャットでやること（優先順）

1. **ドリルをブラウザで通す。** http://localhost:3000/drill → コース1 → map-01 を最後まで。解説と完了（コースに戻る / もう一度 / 次へ）を目視。工房 `/` と［プレビュー］が壊れていないかも見る
2. **図解用キャプチャ。** 工房の3ペイン＋ドリルの5画面。DevTools は閉じる
3. **必要ならコミットしてプッシュ。** ドリル実装に加え、前回からの未コミット（コース3ガイド・aida JSON・参照・locked-principles・HANDOFF 等）が残っている
4. **提出後: コース4 jiyu-01（紬と素材）をグリルしてから生成する。** 問数は `[8, 8, 8]` 仮置き。薄い問で8に伸ばさない。参照は tsumugi-omeshi・sozai-kaku-teire・dansei-kimono。禁止ではなく素材と目的で説明する。紋で礼装へ上げる手順はコース5。ファッション延長と、紬に金袋帯を洒落ものとして勧めることは出さない
5. **コース4 残り**（jiyu-02 小紋 → jiyu-03 街着）。jiyu-02 には edo-sanyaku。江戸三役の形状暗記は背景。色無地で使った「遠目無地＋一つ紋」の焼き直しはしない
6. **コース5**（chosei-01 紋 → 02 帯 → 03 小物）。各回の問数は生成前にグリルする。chosei-01 には kamon-chado と kamon-kiso。chosei-02 には obi-kakuawase。chosei-03 には obijime-obiage。色留袖の紋の数はコース5で再登場。既婚未婚を正しい伺いにするのはコース5か末の上級
7. 必要なら **ガイドのレビュー**（`review-series-guide` / `review-course-guide`）
8. **UIの微調整**（配色は「とりあえずOK」。必要なら相談）
9. **LLM のアプリ内実装**（Agents Bar、最後）
10. **以降は制作者が単独で実施**（キャプチャ・図解・提出）

---

## 未決事項

- ドリルの解答後・完了画面は、次チャットでブラウザ目視が残っている
- コース4〜5の問数は `[8, 8, 8]` 仮置き。生成前にグリルする（提出後）
- 会社パーティの固有文（map-03 Q1 と aida-02 Q3 の差は出稿時に目視）
- aida-03 Q3 茶席と map-03 Q2（お茶で色無地一つ紋）の差は出稿時に目視
- aida-03 Q4（友人の結婚式に一つ紋の色無地）は aida-01 Q1（友人の結婚式に訪問着）と場が近い。直すかは制作者判断。無断修正しない
- `sozai-kaku-teire.txt` は1.2割愛・綿の途中まで。続きを足すかは任意
- 品目そのものシリーズ（絵羽・柄の位置など見た目の識別）は別途用意する予定。範囲と開始時期は未決
- aida-01 承認稿の残件: Q3（紋を入れない理由）と Q4（絵羽）が「品目そのものは別シリーズ」と食い違う。直すかは制作者判断。無断修正しない
- aida-02 承認稿の残件: 色無地がダミーに出る（ガイドは設問に出さない）。準礼装・略礼装が解説に出る。無断修正しない
- 各コースのレッスン本数・タイトルの最終確定（今は各3本の叩き台）
- コース4〜5ガイド本文の詰め（コース3の付下・色無地段落は書いた。4〜5は叩き台）
- Quiz Studio の 6本柱の具体的な中身（参照スキルに記載なし・照合未完了）
- 図解の提出形式・提出先（講師から指定があるはず）
- 慶事以外の場面別コース（弔事専門・茶道など）はこのシリーズに足さない。別判断（思想②の続き）
- 承認済みの残件: kitei-03 Q1（息子の結婚式に父。01の母と近い）。無断修正しない
- Anthropic APIキーは取得済み（生成ボタン用）

---

## 既知の制約・注意事項

- **APIキーはページを閉じると消える**（ファイル保存なし。意図的な設計）
- **スクリーンショット時は DevTools を閉じる**（ネットワークタブにキーが見える）
- **モデル名**: `claude-opus-4-5`（`lib/prompt.ts` の `MODEL` 定数で変更可）
- **生成ロック**: 同一コース内の前レッスンが「これでよい」になるまで、そのコースの次レッスンは生成ボタン不可
- **生成 API**: `{ lessonId, apiKey, scope: 'all' | 'failed' }`。生成ボタンは `onClick={() => onGenerate('all')}`。`onClick={onGenerate}` だとクリックイベントが scope になり `JSON.stringify` が壊れる（修正済み）
- **現在のブランチ**: `feature/beyond-mvp`（提出時は `git checkout main` で戻る）
- **DBはgit管理外**: `data/app.db` はブランチをまたいで共有。シード元JSONを直したら `pnpm seed` でDBへ反映（**既存DBをDROPして入れ直す**）
- **save-guide のコース保存**は `{ type: "course", courseId, guide }`。`courseId` なしは不可
- 旧フォルダ `data/kakuto-tpo/kimono-kaku/` は残しているが seed は `courses/` だけ読む
- **API は DB だけを更新する。** JSON は seed 元。エージェントがファイルにも書く。片方だけ直すと `pnpm seed` で戻る
- JSONを直したら DB へは `save-guide` / `save-lesson`（または `pnpm seed`。seed は DROP）

---

## 後日追加予定（MVP外）

- P4 AI壁打ちチャット（ガイド内容や設問について Claude に相談）
- 設問タイプ：手順（3〜4項目を正しい順に並べる）
- レスポンシブ対応
