# 引き継ぎ文：呉服Quiz工房

## このドキュメントの位置づけ

卒業制作「呉服Quiz工房」の要件整理セッション（grill-me）の成果物です。
実装はまだ始まっていません。次のチャットはこの内容を前提に、
**データ構造と画面の流れのアスキー確認 → モックアップ**へと進んでください。

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| システム名 | 呉服Quiz工房（制作スタジオ） |
| 成果物の名称 | 呉服販売員ドリル |
| 制作者 | 百貨店呉服部の部門長（本人のみ） |
| 参考モデル | 本気AIドリル＋Quiz Studio（Next.js製社内ツール） |
| 期限 | 2026年8月28日（残り10日） |
| 提出物 | 図解のURL（スクリーンショット＋説明。アプリのURLは不要） |

---

## 技術スタック

- **フレームワーク**: Next.js + shadcn/ui + Tailwind CSS
- **起動**: `pnpm dev`、ローカルのみ（Vercel不要）
- **AI**: Anthropic Claude API を直接呼ぶ（Agent SDKはMVPでは使わない）
- **APIキー**: 画面の設定欄に貼るだけ。ファイルにもコードにも書かない
- **データ**: JSONファイルをGitリポジトリに保存。ブラウザストレージは使わない

---

## コンテンツの4階層

```
呉服販売員ドリル（総称）
└ シリーズ（6〜8コースで構成）
     例：格とTPO ／ 長襦袢 ／ お手入れ
  └ コース（6〜8レッスンで構成）
       例：着物の格 ／ 帯の格 ／ きものと帯合わせ
    └ レッスン（8〜12設問で構成）
         例：第一礼装 ／ 準礼装・略礼服 ／ しゃれもの
      └ 設問（四択）
```

---

## MVPの範囲

**1シリーズ・1コース・1レッスン・8設問** を6工程で通す。

- シリーズ：格とTPO
- コース：着物の格
- レッスン：1本（第一礼装が含まれるレッスン）
- 設問：四択＋短い問題文・写真なし

画面上には2本目・3本目のレッスン枠を出すが、前のレッスンが「これでよい」になるまで生成できない。

---

## 6工程（レッスン1本を作る作業手順）

1. シリーズガイド＋コースガイドを書く（必須欄が空なら生成ボタンは押せない）
2. レッスンを選んでAIに下書きを生成させる
3. プログラムが形式を検査する
4. 画面で悪い問だけ手で書き直す（レッスン全体の再生成は確認ありでのみ可）
5. 「これでよい」を押す（次のレッスン生成の参照元になる印。公開ではない）
6. プレビューで8問を解く（下書き状態でも可）

---

## ガイドの項目（人間が先に書く正本）

### シリーズガイド

- 対象者
- 目的
- 使う用語
- 補助語「段」の定義
- 禁止する言い換え（フォーマル度・ドレスコード・ランク・レベル）
- 例外（振袖と留袖の上下比較は禁止。色無地は紋で段が動く。紬は格と価格を混ぜない）
- 文体

### コースガイド

- 各レッスンの役割
- 各回で必ず出すこと
- 後のレッスンで再登場させること
- 含めないこと（話法・価格・着付け）
- 完了条件

---

## 機械検査の範囲

- 設問数が8問か
- 選択肢が4つか
- 正解が1つか
- 解説があるか
- 禁止語が含まれていないか

呉服の正しさ・再登場の確認はあなたが目で見る。

---

## プレビューの動作

- 解答直後に正誤＋解説2〜3文を表示
- 点数の保存なし
- 承認前でも自分は解ける（検品用）

---

## 想定学習者

- 主対象：他部門からの異動者（接客はできるが呉服用語は初めて）
- 新卒が知らない用語は問題文の中で一度だけ定義する
- 新卒用・異動者用の別コースは作らない

---

## エラー時の動作

- 生成失敗・形式エラー時：今の下書きはそのまま残す。理由を表示して「再生成」だけ出す
- 自動リトライ・別AIによる修理は行わない

---

## Quiz Studioから学んだこと（講義の確認事項）

- Quiz Studioも最初は「Quiz Viewer」（表示だけ）から始まった
- データはJSONファイルでリポジトリ管理（ブラウザストレージではない）
- AIはUI上のチャット欄から呼ぶ（CursorやターミナルではなくUIに組み込む）
- 最初からUI上のAI機能を作ろうとするのはコスパが悪い、と講師が明言している
- したがって推奨は「表示→編集→AI生成」の順で積み上げること

---

## 前回セッションで出したアスキー（要修正）

> **注意**: このアスキーは4階層への修正が確定する前に書かれたため、
> 階層の呼び名がずれています。次のチャットで「どこを直すか」を確認してから先へ進む。

### ずれのポイント

| アスキー上の名前 | 正しい名前 |
|------|------|
| シリーズ 呉服販売員ドリル | 呉服販売員ドリル（総称。シリーズではない） |
| コース 格とTPO | **シリーズ** 格とTPO |
| レッスン1 着物の格 | **コース1** 着物の格 |
| 問題 × 8 | **レッスン** × 8設問 |

### 前回のアスキー（参照用）

**データ（入れ子）**

```
シリーズ  呉服販売員ドリル          ← 「総称」が正しい
├ 全体ガイド
│  対象者 / 目的 / 用語 / 補助語「段」
│  禁止言い換え / 例外 / 文体
│
└ コース  格とTPO                   ← 正しくは「シリーズ」
   ├ コースガイド                   ← 正しくは「シリーズガイド」
   │  3レッスンの役割
   │  各回で必ず出すこと
   │  3本目で再登場させること
   │  含めないこと / 完了条件
   │
   ├ レッスン1  着物の格            ← 正しくは「コース1」
   ├ レッスン2  帯と合わせ          ← 正しくは「コース2」
   └ レッスン3  TPO＋再登場         ← 正しくは「コース3」

レッスン1本                         ← 正しくは「コース1本」の中の「レッスン」
├ 状態  未生成 / 下書き / これでよい
├ 検査  未実施 / 合格 / 不合格（理由）
├ 生成エラー（あれば、下書きは触らない）
└ 問題 × 8                         ← 正しくは「設問 × 8」
   問題文 / 選択肢4 / 正解1つ / 解説2〜3文

設定（ファイルに出さない）
└ APIキー
```

**画面と操作**

```
[設定] APIキーを入れる
   │
[シリーズ] 全体ガイドを書く ── 書き出し / 読み込み
   │  必須が空でも保存は可。生成は不可
   ▼
[コース] コースガイドを書く
   │
   ▼
[レッスン一覧]
   1 着物の格      未生成 | 下書き | これでよい
   2 帯と合わせ    （1がこれでよいなら生成可）
   3 TPO＋再登場   （1と2がこれでよいなら生成可）

   各行: [生成] [編集] [プレビュー]
```

```
[レッスン編集]  ← 生成の直後も、あとからの直しもここ

  [レッスンを再生成] ← 今の下書きを捨てる確認あり
  検査: 合格 / 不合格（8問・四択・正解1・解説・禁止語）
  [これでよい] ← 検査合格のときだけ

  Q1 問題文
     ( ) ( ) ( ) ( )  正解を1つ
     解説
  Q2 ...
  Q8
```

```
[プレビュー]  ← 下書きでも可。公開ではない

  問 3 / 8
  問題文
  四択 → 答える
  正誤 + 解説2〜3文
  [次へ]
```

**操作の一本（修正前の表記のまま）**

```
ガイドを埋める
 → レッスン1を生成          ← 正しくは「コース1のレッスンを生成」
 → 悪い問だけ自分で直す
 → 検査に通す
 → 「これでよい」
 → プレビューで解く
 → レッスン2へ              ← 正しくは「次のコースへ」or「コース1の次のレッスンへ」
```

---

## 次のチャットでやること（優先順）

1. **アスキー（データ構造と画面の流れ）を確認・修正する**（実装はしない）
   - 上記の「ずれのポイント」を踏まえ、4階層に合わせたアスキーを書き直す
   - 画面構成もあわせて「ガイドとレッスン一覧を1画面にまとめる」方向で整理する
   - 確認が取れたら先へ進む
2. Tailwind CSS でモックアップ（見た目だけ）を作る
3. Next.js でMVP（動く）にする
4. スクリーンショットを撮る
5. 図解を作って提出

---

## 未決事項（実装後にあなたが決める）

- ガイドに書く呉服の正本の中身（用語・例外の具体的なテキスト）
- Anthropic APIキーの発行（まだ。[console.anthropic.com](https://console.anthropic.com) で発行する）
