# 引き継ぎ文：呉服Quiz工房

## このドキュメントの位置づけ

前回のセッション（grill-me → アスキー確認 → モックアップ → MVP実装）の成果物です。
**アプリは実装済み・未テスト**の状態です。

### AIとの共同作業の範囲
- **共同**: 動作確認・バグ修正・UI改善・プロンプト調整・Quiz工房のブラッシュアップ
- **制作者が単独で行う**: ガイドの中身を書く・画面キャプチャ・図解作成・提出

次のチャットは「動作確認 → バグ修正 → ガイド記入 → 生成テスト → ブラッシュアップ」へ進んでください。

---

## プロジェクト概要

| 項目 | 内容 |
|------|------|
| システム名 | 呉服Quiz工房（制作スタジオ） |
| 成果物の名称 | 呉服販売員ドリル |
| 制作者 | 百貨店呉服部の部門長（本人のみ） |
| 期限 | 2026年8月28日 |
| 提出物 | 図解のURL（スクリーンショット＋説明。アプリのURLは不要） |

---

## 技術スタック

- **フレームワーク**: Next.js 16 + Tailwind CSS 4
- **AI**: Anthropic Claude API（`@anthropic-ai/sdk`）。モデル: `claude-opus-4-5`
- **起動**: `pnpm dev`（ワークスペースルートで実行）→ http://localhost:3000
- **APIキー**: 画面の設定欄（⚙）に貼るだけ。ファイルに書かない・保存されない
- **データ**: `data/` ディレクトリの JSON ファイルで管理

---

## ファイル構成

```
Graduation-work/
├── app/
│   ├── api/
│   │   ├── data/route.ts           GET  全データ読み込み
│   │   ├── save-guide/route.ts     POST シリーズ/コースガイド保存
│   │   ├── generate/route.ts       POST Claude API でレッスン生成
│   │   ├── save-lesson/route.ts    POST 設問保存（検査も再実行）
│   │   └── approve-lesson/route.ts POST「これでよい」承認
│   ├── preview/[lessonId]/page.tsx プレビュー（1問ずつ解く）
│   ├── globals.css
│   ├── layout.tsx
│   └── page.tsx                    3ペインメイン画面
├── data/
│   └── kakuto-tpo/
│       ├── series.json             シリーズガイド（現在は空欄）
│       └── kimono-kaku/
│           ├── course.json         コースガイド（現在は空欄）
│           └── lessons/
│               ├── lesson-01.json  第一礼装（status: pending）
│               ├── lesson-02.json  準礼装・略礼服（status: pending）
│               └── lesson-03.json  しゃれもの（status: pending）
├── lib/
│   ├── types.ts                    型定義
│   ├── data.ts                     JSON読み書きヘルパー
│   ├── inspect.ts                  機械検査ロジック
│   └── prompt.ts                   Claude プロンプト構築
├── HANDOFF.md                      このファイル
└── package.json
```

---

## コンテンツの4階層

```
呉服販売員ドリル（総称）
└ シリーズ  格とTPO
   ├ シリーズガイド（人間が先に書く）
   └ コース1  着物の格
      ├ コースガイド（人間が先に書く）
      ├ レッスン1  第一礼装
      ├ レッスン2  準礼装・略礼服
      └ レッスン3  しゃれもの

各レッスン
├ 状態: 未生成 / 下書き / これでよい
├ 検査: 未実施 / 合格 / 不合格（理由）
├ 生成エラー（あれば下書きは触らない）
└ 設問 × 8
   ├ [四択]  問題文 / 選択肢4 / 正解インデックス / 解説
   └ [○×]   問題文 / 正解（true/false）/ 解説
   （手順タイプは後日追加。MVPには含まない）
```

---

## UIの画面構成（3ペイン）

```
┌──────────────┬────────────────────────┬────────────────────────┐
│ P1 ツリー    │ P2 ガイド              │ P3 編集 / 設問          │
│ （左サイド） │ （中央）               │ （右メイン）            │
│──────────────│────────────────────────│────────────────────────│
│▼ 格とTPO    │← シリーズ選択:        │← レッスン選択:        │
│  ▼ 着物の格  │  シリーズガイド編集    │  設問一覧              │
│   □ レッスン1│                        │  [生成] [再生成]       │
│   □ レッスン2│← コース選択:          │  検査: 合格 / 不合格   │
│   □ レッスン3│  コースガイド編集      │  [これでよい]          │
│              │                        │  [プレビュー]          │
│[⚙設定]      │← レッスン/設問選択:   │← 設問選択: 1問編集     │
│              │  コースガイドを参照表示│                        │
└──────────────┴────────────────────────┴────────────────────────┘
凡例: □未生成  ○下書き  ●これでよい
```

P4（AIチャット壁打ち）は **後日追加**。MVPには含まない。

---

## 6工程（レッスン1本を作る手順）

1. シリーズガイド＋コースガイドを書く（P1でシリーズ/コースを選択 → P2で記入 → [保存]）
2. レッスンを選んで [このレッスンを生成]（APIキーが必要）
3. プログラムが形式を自動検査する（8問・四択/○×形式・解説・禁止語）
4. 悪い設問だけ P1 でクリック → P3 で手直し
5. 検査合格後に [これでよい ✓]（次のレッスン生成の参照元になる）
6. [プレビュー ▶] で8問を解く（検品用。点数は保存されない）

---

## 機械検査の内容

| 検査項目 | ルール |
|---|---|
| 設問数 | 8問であること |
| 四択 | 選択肢が4つ・正解インデックスが0〜3 |
| ○× | answerが true か false |
| 解説 | 全設問にあること |
| 禁止語 | フォーマル度・ドレスコード・ランク・レベル を含まない |

呉服の内容の正しさ・再登場の確認は **制作者が目視**で行う。

---

## 現在の状態

| 項目 | 状態 |
|---|---|
| Next.js プロジェクト | ✅ 作成済み |
| 3ペインUI | ✅ 実装済み |
| Claude API 生成 | ✅ 実装済み（未テスト） |
| 機械検査 | ✅ 実装済み |
| 設問編集 | ✅ 実装済み |
| 承認（これでよい） | ✅ 実装済み |
| プレビュー | ✅ 実装済み |
| シリーズ/コースガイドの中身 | ❌ 空欄のまま（制作者が書く） |
| 動作確認（E2Eテスト） | ❌ まだ |
| スクリーンショット | ❌ まだ |
| 図解（提出物） | ❌ まだ |

---

## 次のチャットでやること（優先順）

1. **`pnpm dev` を起動して動作確認**
   - APIキーを設定欄に入力
   - ガイドを空のままで「生成」を押してみる（動くかの確認）
   - エラーが出たら一緒に直す

2. **シリーズガイド・コースガイドを書く（制作者の作業）**
   - 対象者・目的・補助語「段」の定義・禁止語の例外 等
   - ここが充実するほど Claude が出す設問の品質が上がる

3. **本番ガイドで再生成 → 設問を確認・手直し → 「これでよい」**

4. **Quiz工房のブラッシュアップ（共同作業の最終ゴール）**
   - プロンプトの品質調整（設問のトンマナ・難易度・段の使い方）
   - UIの気になる点を修正
   - ここまでが共同作業の範囲

5. **以降は制作者が単独で実施**
   - 画面キャプチャ
   - 図解作成・提出

---

## 未決事項

- ガイドに書く呉服の正本の中身（用語・例外の具体的テキスト）
- 図解の提出形式・提出先（講師から指定があるはず）
- Anthropic APIキーは取得済み

---

## 既知の制約・注意事項

- **APIキーはページを閉じると消える**（ファイル保存なし。意図的な設計）
- **スクリーンショット時は DevTools を閉じる**（ネットワークタブにキーが見える）
- **モデル名**: `claude-opus-4-5`（`lib/prompt.ts` の `MODEL` 定数で変更可）
- **レッスン2の生成ロック**: レッスン1が「これでよい」になるまで生成ボタンは押せない

---

## 後日追加予定（MVP外）

- P4 AI壁打ちチャット（ガイド内容や設問について Claude に相談）
- 設問タイプ：手順（3〜4項目を正しい順に並べる）
- レスポンシブ対応
